import React from 'react';
import '../styles/Spinner.css'; // Ensure you create a CSS file for styling

const Spinner = () => {
    return (
        <div className="spinner-container">
            <div className="spinner"></div>
        </div>
    );
};

export default Spinner;
